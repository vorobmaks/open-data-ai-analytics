apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: open-data-ai-analytics
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/vorobmaks/open-data-ai-analytics.git
    targetRevision: main
    path: gitops/app
  destination:
    server: https://kubernetes.default.svc
    namespace: open-data
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
