apiVersion: v1
kind: Service
metadata:
  name: open-data-web-service
  namespace: open-data
  labels:
    app: open-data-web
spec:
  type: NodePort
  selector:
    app: open-data-web
  ports:
    - name: http
      port: 80
      targetPort: 5000
      nodePort: 30080
